#!/bin/sh

ACTUAL_OUT=$TEMP_DIR/check.out
EXPECTED_OUT=$PROJECT_DIR/test/check.out
PROGRAM=$BUILT_PRODUCTS_DIR/FiendsAndFortune

mkdir -p $TEMP_DIR
$PROGRAM check 0 > $ACTUAL_OUT
diff $ACTUAL_OUT $EXPECTED_OUT
